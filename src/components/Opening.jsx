import React from 'react';
import Typed from 'typed.js';

export default function typeOpening() {
  // Create reference to store the DOM element containing the animation
  const el = React.useRef(null)
  const typed = useRef(null)



  return (
    <div>
        
    </div>
  )
}

